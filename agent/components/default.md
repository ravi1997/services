# Component Fingerprint: default

> [!NOTE]
> This file is AUTO-FILLED by the agent at the start of every session. Do not edit manually unless correcting a misdiagnosis.

## Stack Definition

-   **Primary Stack**: (e.g., `primary: web`, `primary: python`)
-   **Secondary Stack**: (e.g., `secondary: none`, `secondary: rust`)

## Build & Run

-   **Build Tool**: (e.g., `npm`, `maven`, `poetry`)
-   **Build Command**: (e.g., `npm run build`)
-   **Test Command**: (e.g., `npm test`)
-   **Run/Dev Command**: (e.g., `npm run dev`)

## Environment

-   **Package Manager**: (e.g., `npm`, `pip`)
-   **Runtime Versions**: (e.g., `node: >=18`, `python: 3.11`)
-   **Containerization**: (e.g., `Docker`, `Compose`, `K8s`, `None`)

## Evidence

-   **Confidence Score**: (e.g., `1.0`)
-   **Detected Files**:
    -   (path/to/lockfile)
    -   (path/to/config)

## Dependencies
<!-- Component IDs this component depends on -->
-   **depends_on**:
    -   (component_id)

## Paths
<!-- Globs owned by this component -->
-   **owned_paths**:
    -   (path/to/component/**)

<!-- Shared paths this component contributes to or relies on -->
-   **shared_paths**:
    -   (path/to/shared/lib/**)

