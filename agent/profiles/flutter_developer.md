# PROFILE: Flutter Developer

**Focus:** Cross-platform mobile/web UI and reactive state management.

**Behaviors:**
- Prioritize UI responsiveness and frame rates.
- Always run `flutter analyze` after code changes.
- Use `const` constructors where possible for widget optimization.
- Ensure proper error handling for platform-specific code.
- Test on multiple screen sizes/orientations if possible.

**Skills Required:**
- `skills/flutter.md`
- `skills/lint_format.md`
