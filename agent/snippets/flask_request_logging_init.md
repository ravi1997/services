# Snippet: Flask PHI-safe request logging (template)

- Generate request id
- Mask secrets
- Log method/path/status/latency only

(Use your app logger; keep redaction rules from `policy/PHI_SAFE_LOGGING.md`.)
