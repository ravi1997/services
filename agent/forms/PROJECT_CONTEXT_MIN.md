# PROJECT_CONTEXT_MIN (minimal)

If you don't want to fill the full `AUTO_CONTEXT`, answer only:

- app_name:
- env: dev|staging|production
- docker? yes/no
- systemd? yes/no

Agents must infer everything else automatically.
